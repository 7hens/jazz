# 手工走查清单(walkthrough)

> **这是「人眼验收」的唯一清单** —— 本仓没有 headless 浏览器、且**禁止截图**(见 `CLAUDE.md` 红线),所以**布局 / 手势 / 动画 / 持久化**这类只能由人在浏览器里判。
> **单一事实源**:`.claude/skills/release/SKILL.md` 的闸门①**只指向本文件**,不再内嵌条目 —— 两处写一份必漂移。
> **每次发布前必走**;日常改功能按 §D 的规则**在同一个提交里**维护它。

---

## A. 条目写法(四条硬要求,改本文件时逐条对照)

1. **必须是人眼可判定的现象**。「应该没问题」「正常显示」等于没写;要写成「松手落在放不下的槽上 → **该槽红一下**,积木**不回槽也不落位**」。
2. **必须写清前提**:哪台设备、什么宽度、要不要先刷新。**做不到前提的人不算通过,只算「未验」**(见 §D 第 4 条)。
3. **不许依赖截图**:本仓禁止截图/读图检查 UI。判据要能用**看一眼、听一声、点一下**得出。
4. **一条只判一件事**;两件事写两条(这样红了才知道红在哪)。

**代码侧已守、此处只判其人的后果**(别去读断言来推断视觉结果):`src/features/pinyin-blocks/PinyinBlocksGame.tsx` 的拖拽(含 `pointercancel`、二指同按)、结算 260ms 窗口;`src/features/pinyin-blocks/rules.ts` 的槽位/干扰块/星级;`worker/pinyin-progress.ts` 的 MAX 合并。

---

## B. 核心回归 —— **每次发布前逐条走完**

> 今天共 **8 条**。全部走完 + 明确「通过」,闸门①才开。

| # | 前提 | 操作 | 可判定的期望 |
|---|---|---|---|
| **W-C1** 登录 | 任意设备 | 输入家长通行令牌登录 | 落在**单元地图**;**只有第 1 单元可点**,第 2 单元起带 🔒 |
| **W-C2** 进关与拖拽主路径 | 任意设备 | 进第 1 单元(右上角显示 **1/3**)→ **按住**一块积木拖动 | 拖动中**幽灵块跟着手指/鼠标走**;松手**落进槽里**;**落在放不下的槽**上则不落位、**该槽红一下**;**落在空处**则什么都不发生 |
| **W-C3** 手势被系统收走 | **真触摸设备**(见下注) | 拖到一半**切后台 / 被系统收走手势**(`pointercancel`) | **幽灵块当场消失**,**源块的压暗同时归零**(不能停在「半透明」态) |
| **W-C4** 两指同按 | **真触摸设备**(见下注) | 一指拖着积木时,**另一指再按下** | 第二指按下的瞬间**第一指的幽灵块消失**;**屏幕上任何时刻至多一个幽灵块** |
| **W-C5** 结算窗口内抢拖 | 任意设备 | 拼齐最后一块(**含声调块**)→ 答案行亮出拼音 + 汉字并朗读,**紧接着再拖一块**(结算动画约 **260ms**,抢在它前面) | **不卡死、不丢块**;随后**照常进下一关** |
| **W-C6** 发音 | 任意设备(需有声音) | 听 W-C5 的那次朗读 | 读的是**汉字读音**(同音汉字),**不是逐字母念拉丁串** |
| **W-C7** 刷新与持久化 | 任意设备 | **刷新页面** → 回地图看刚过的关 | 仍是**登录态**;星星**亮着**、该单元进度与刷新前**一致** |
| **W-C8** 解锁 | 任意设备 | 把第 1 单元 **3 关全通** | 自动**回地图**:该单元 **3/3**、三个星位**全亮**,第 2 单元的 🔒 **消失且可点进去** |

> **W-C3 / W-C4 的注**:两条**要真触摸设备**(平板/手机)。④ 要两根**同时**按下的指头 —— **鼠标只有一个指针**;③ 要「系统收走手势」的那一下,**本仓没在真机上验过怎么触发**,所以这两条**只给前提、不给操作配方**。
> **手上没有触摸设备 → 把「W-C3/W-C4 未验」写进闸门结论,不要据此判「通过」。**

---

## C. 分区扩展项 —— **按本次改动面选走**

> 核心回归是「每次必走的最小集」;下面按**能力面**分组,**改了哪一面就补走哪一面**。功能新增时**往对应分区里加条目**。

### 布局与窄屏
| # | 前提 | 操作 | 期望 |
|---|---|---|---|
| **W-V1** 窄屏地图 | 窗口 **640px** 与 **375px** 各看一次 | 看单元地图格子 | 徽章行**正常换行、不溢出** |
| **W-V2** 窄屏积木盘 | 同上 | 看关内积木盘 | 积木块**不被压扁**(还是方的,**没被 flex 挤成细条**) |

### 地图
| # | 前提 | 操作 | 期望 |
|---|---|---|---|
| **W-M1** 锁着的格子点了没反应 | 先登录,停在地图上(只有 u1 可点,第 2 单元起带 🔒) | **点第 2 单元(带 🔒 那个)** | **什么都不发生** —— 不进关、**不抖、不响**、格子外观不变。依据:本版未实现「抖动 + 提示音」(spec §6.2 那句「复用 `.pblock--buzz`」从未接线,该规则已随 T15 从 `src/index.css` 删除),`UnitMap` 里就是 `if (locked) return`。**点完仍停在地图上**才算通过 |

### 关卡与积木盘
| # | 前提 | 操作 | 期望 |
|---|---|---|---|
| **W-L1** 声调块单独成行 | 任意设备,进任一关 | 看积木盘 | **组合块一行、声调块单独一行**(有分隔),**不混着放** |
| **W-L2** 双音节词两组拼装组 | 进到有双音节词的关卡 | 数拼装组 | 有**两组**(不是一组挤 4 个槽) |
| **W-L3** 提示三档 | 进任一关,故意连错 | 观察槽的提示浓淡 | **三档递减**(强 > 中 > 弱);连错 2 次**回到强档** |

### 结算与奖励
| # | 前提 | 操作 | 期望 |
|---|---|---|---|
| **W-S1** 星级口径 | 任意设备 | 在第 1 单元分别以 **0 错 / 1-2 错 / ≥3 错** 各通一关 —— 拼齐后**自动**进下一关,想看地图就点关卡页左上角 **←**(第 3 关通完**自动**回地图) | 三次都**照常过关** —— **≥3 错也不出现「失败」**(地图上没有失败标记);回地图后该关的星位**点亮**、格子下方的 `n/3` 加一 |
| **W-S2** 星尘只升不降 | 任意设备 | 同一关**重打一次,拿更少星** | 该关星级**不被降**;星尘**不减少** |

> **W-S1 的注(为什么期望里没有「0 错 = 3 星」)**:地图上**一个星位只表示「这一关通了」**(`.pstar` 亮/暗两态),**看不出某关是几星**;关内也没有星级揭晓(T15 勘误:spec §7.4 的「星级揭晓 + 继续按钮」本版未实现)。所以「0 错 = 3 星 / ≤2 错 = 2 星 / 其余 1 星」这条口径**今天没有可判定的人眼出口** —— 它由 `npm test` 的 `rules.ts` 星级纯逻辑(`starsFor`)守,不属本文件射程。本条目只判人眼能判的那一半:**三档错数都照常通关,不出现「失败」**。缺口登记见本节末「尚未纳入」。

### 家长面板(**这一页可以写字** —— 给大人用的,不是给孩子用的)
| # | 前提 | 操作 | 期望 |
|---|---|---|---|
| **W-P1** 打开与关闭 | 先登录,停在地图上 | 从**地图上的家长入口**打开面板 | 面板出现(标题「家长设置」);**点遮罩 / 右上 ✕ / 「完成」三种方式都能关掉** |
| **W-P2** 退出登录 | 同上,面板内 | 点「退出登录」 | 回到**登录门**;**刷新后仍停在登录门**(登出是服务端 `Set-Cookie` 清掉的,**不是**只清了前端状态) |
| **W-P3** 重置全部进度 | 同上,**先确保已有点亮的星星和星尘** | 点「重置全部进度」 | **先弹确认框**;点**取消** → **星星、星尘一个不少**;再点一次并**确认** → **全部清零**,地图回到初始(**第 2 单元起重新带 🔒**) |

### 尚未纳入(诚实缺口 —— 补条目或放弃,别默认它们被守住了)
- **关卡内的星级 / 连击即时反馈**:本版**没有** —— 关内不显示星、不揭晓星级,拼齐后是**自动推进**(末关自动回地图);星只在地图格子上有出口,且只有**点/灭两态**(看不出 1–3 星)。**未纳入**(要补条目就得先有可判定的现象);已进 `docs/PLAN.md` 想法池。
- **成就 / 连击 / 幸运奖励的可见反馈**:结算里触发,但**屏幕上能不能看出来、什么时候看出来**,未核 → **未纳入**。
- **「零文本」这条设计不变量**:「没有文字」与「没有**必须读懂才能玩**的文字」是两回事,**今天没有可判定的写法** → 要么补一条可判定的,要么**明确放弃它作为闸门**。
- 任何**只有自动化测试守着**的东西(纯逻辑、服务契约、架构边界)不进本文件 —— 那些由 `npm test` 守。

---

## D. 维护规则(让这份文件活下来的机制)

1. **谁改功能谁改文档,同一个提交。** 提交里凡触碰**用户可见行为**(样式 / 布局 / 手势 / 动画 / 文案 / 持久化 / 发音),`git diff` 里**必须同时出现本文件**。
2. **触发条件表**:
   | 发生了什么 | 本文件要做什么 |
   |---|---|
   | 新增用户可见功能 | **加条目**(进对应分区;若属最小闭环,进 §B) |
   | 改用户可见行为 | **改条目**(把期望改成新事实 —— 旧的期望**今天就假**了) |
   | 删功能 | **删条目**(留着就是僵尸条目) |
   | 改设备/宽度/前置条件 | **改「前提」列** |
   | 只想「顺手优化几句措辞」 | **不动** —— 本文件不是散文 |
3. **走哪一档(可判定,不靠自觉)**:
   | 时机 | 走多少 |
   |---|---|
   | **每次发布前**(`preview` 冒烟,闸门①) | **§B 核心回归**(8 条) |
   | **合并进 `main` 之前** | **§B + §C 全量** |
   | 日常改动(不发布) | 本次**碰到的那个分区**必走 |
4. **闸门口径**:条目只有三种结果 —— **通过 / 未通过 / 未验**(做不到前提)。**「未验」不能记成「通过」**;`W-C3/W-C4` 缺设备时按此记。**判定人 = 走查的人;发布人负责把结论记下来。**
5. **结论记在哪**:每次发布的走查结果写进根 `CHANGELOG.md` 对应发布条目(复盘三问已在那里),**不写进本文件** —— 本文件只放**当前**该走的清单,历史不进正文(否则会被历史撑爆、条目失焦)。
6. **发布流水线引用**:`.claude/skills/release/SKILL.md` 的闸门① = 「给用户 preview URL + 本文件 §B 的核心回归」;**等用户明确「通过」**;不因 `preview == prod`、时间紧、用户催而跳过。
7. **复审红线**:整支终审 / 复审把「改了用户可见行为却没动本文件」当**一条要报的项** —— 这是本文件不被时间磨掉的唯一保障。

---

## E. 为什么这些必须人走

`npm test` 跑在 **jsdom** 上:**没有 CSS/布局引擎,没有指针事件序列**,所以
- 「块被 flex 压扁」「徽章行溢出」 → **jsdom 里根本不可断言**;
- 「幽灵块跟手」「第二指按下时第一指消失」 → **DOM 结构可断言,视觉后果不可**;
- 「刷新后仍是登录态」 → 涉及真 cookie 与真 worker,**只有真浏览器**。

代码侧能守的部分(结构、类名、变量、MAX 合并)已经在 `npm test` 里,**别在这里重复它们** —— 本文件只放**机器判不了**的那些。
