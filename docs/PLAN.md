# PLAN 需求与任务管理

> 全仓需求/任务**唯一入口**,取代 `docs/IDEAS.md`(想法池)+ `docs/TODO.md`(看板)。规则:
>
> - 任何新需求先进「想法池」**一行,带 `P0`/`P1`/`P2` 前缀**;重复即弃旧;**禁止直接开工**。
> - 排进本轮开工 → 升「当前迭代」;确认不做 → 移「坚决不做」留痕(防反复)。
> - 状态细节在 spec / plan / sdd,此处只记**行级事实 + 优先级**,不逐条抄进度。
> - 关键准则(准入 / 估算收尾 / 复盘三问 / 分支模型):见 `CLAUDE.md`「需求与版本管理」。

## P 优先级(需求 = 影响面,P = 何时做)

| 级 | 含义 | 闸门 |
| :-- | :-- | :-- |
| `P0` | 最高:本轮在跑 / 阻塞发版 | 立即做,做完发 |
| `P1` | 确认要做,待排期(原「二期 backlog」) | 立项 → 排期 → 开工 |
| `P2` | 想法观察,未承诺 | 先入池冷静;重复即弃 |

## 当前迭代

> 双轨制,可并行(轨 = 变更类型/发布线,版本号由影响面定,见 CLAUDE「版本语义」):
>
> - **feature 轨** — 新能力/需求线,可**聚合多个需求**并行开发;集齐、双闸门过才发,不逐需求发。版本:兼容新增 → minor;破坏 → major(1.0.0 起,0.x 阶段破坏仍落 minor)。
> - **hotfix 轨** — bug 修复线,基于**已发布 tag** 出,不夹带未发 feature 代码;就绪即发独立 patch。
>
> 行格式:`- [ ] P<n> [轨] 标题 — <状态> · <链接>`;详设 `superpowers/specs/`、执行 `superpowers/plans/` + `.superpowers/sdd/`,轨内可多 plan。发版后清空对应轨换新、版本前推。

### feature 轨 — 目标 `0.2.0`(未发;新能力,兼容 → minor)

> 🔴 **2026-09-21 轨整并**:产品口径「**开始落实到真正的游戏里**」+「**原来的东西都推倒重来**」。
> 拼音积木试验田扶正为**唯一玩法**,本轨旧条目与下行 `0.3.0` **全轨**一并作废(行就地留痕,不删)。
> 版本轨**继续沿用 `0.2.0`** —— 不新开轨、不重编版本号。迁移账见下行「远程 D1 迁移」行的裁定结果。

- [x] `P0` `[feature]` 拼音积木取代全游戏(去故事化 · 纯拼音 · 单向课程)— 试验田(`?mock=pinyin`,零持久化)扶正为唯一玩法,千字谷 / 字母林 / 词课 / 基础引导 / 出题引擎 / 词库整批删除。骨架:单元地图(7 格**名片块**,零文本自表意)+ 单元内线性 + 每关星级 1-3 + 星尘全局累计;迁移 `0008_pinyin_progress`(每 user 一行,星级 JSON + 星尘,两条线单调升走 MAX 合并,worker 读-改-写);关卡加**显式稳定 id**(存档只认 id,顺序可调);三档提示收敛成两个 CSS 变量、按单元固定撤 + 连错 2 次回强;连击 / 成就 / 幸运奖励**全留**、触发点从词课结算重锚到关卡结算(成就目录 5/8 条换锚点)。**分两次提交先建后删**,删除为独立可 revert 提交。**本行取代本轨全部旧条目与 `0.3.0` 全轨** — [design](superpowers/specs/2026-09-21-pinyin-blocks-full-game-design.md) — ✅ **已合进 `main`**:`feat/pinyin-full-game` → `main`,**合并提交** `00a11b9`(T1–T14e 在分支上完成 —— 每任务复审 + 整支终审 + 唯一一次修复轮 + 修复作用域复审,残余全部收口在 `8332a03`)。**发版闸门未开**:**人眼走查未跑** —— `docs/walkthrough.md` 的 **§B 核心回归 + §C 分区扩展全量**是合并/发布前的判定口径(原先内嵌在 `release/SKILL.md` 里的那 8 条已改指向该文件,单一事实源)。未发版、未打 tag;**发布**是 T15 剩下的事。T14a = `shared/ui` 遗物清扫 + 文档同步,已完成。

- [x] `P0` `[feature]` 趣味性系统(fun-system)— 开发完成并入 main,随 0.2.0 一起发 — plan `2026-09-04-fun-system.md`(已删) / [spec](superpowers/specs/2026-09-04-fun-system-design.md)
- [x] `P0` `[feature]` 前端架构重构 — 开发完成并入 main,随 0.2.0 一起发 — plan `2026-09-04-dev-architecture-refactor.md`(已删) / [spec](superpowers/specs/2026-09-04-dev-architecture-refactor-design.md)
- [x] `P0` `[feature]` 基础引导自适应教学层(foundation-learning)— 开发完成并入 main,随 0.2.0 一起发 — [spec](superpowers/specs/2026-09-05-foundation-learning-design.md) / plan·core `2026-09-05-foundation-learning-core.md` / plan·embed `2026-09-05-foundation-learning-embed.md`(两份 plan 已删)
- [x] `P0` `[feature]` 浏览器端人工验收 — 0.2.0 发布前置:登录 → 词 1 三技能(含故意答错)→ 结算 +110 → 解锁词 2 → 关拼音后词 2 只 2 步 → 刷新持久 → 重置归零;补 fun 抽查(连击 / 成就 / 幸运 / 夸奖 / 撒花)
- [x] `P0` `[feature]` 拼音短教单元锚点重构 — 人工验收发现:合体 chip 读整词(缺音节↔汉字对齐)、j/i 等 9 对声母/韵母共享同锚(emoji/读音不独立)。修:decompose 产音节汉字、catalogs 每单元独立单音锚点、demo/quiz 消费对齐 — [design](superpowers/specs/2026-09-06-pinyin-anchor-redesign-design.md)
- [x] `P0` `[feature]` 答题卡交互重构(点听·确认制)+ 短教结口 — 人工验收:短教全屏无返回出口、教学与做题同款难辨、选项卡喇叭拥挤且点选即跳误触无确认。改:Choice/ListenChoice/Match 去喇叭、点卡即念、题干整块可点重播、「确定」提交才判;短教卡听齐(requireVisitAll)才可确认作答但仍判分保熟度、补「返回地图」出口、教学期视觉壳与真答题区分;主词课判对后自动推进保留(注:短教「听齐」中间态已被下批「答题卡交互收口」改为纯判分 4 选项,见下行) — 开发完成并入 main,随 0.2.0 一起发 — [design](superpowers/specs/2026-09-06-quiz-interaction-redesign-design.md) / plan `2026-09-06-quiz-interaction-redesign.md`(已删)
- [x] `P0` `[feature]` 砍词课步前 soft「想先学一下?」软浮条 — soft(教过仍 learning)不再打断、放行直接答题,仅未教 mandatory 强制短教;改 `FoundationStepGate` 放行 + App judge 收敛 `mandatory` — 已并入 main(commit e04260e),随 0.2.0 发
- [x] `P0` `[feature]` 答题卡交互收口(短教纯判分 + 全题型 4 选项 + 题型徽章 + 听一听喇叭重听)— 家长口径「别听完才答、直接选对错判分」:短教 TeachOverlay 删 demo/tap 演示步与听齐(requireVisitAll)门,对每个未教单元直接出 **4 选项纯判分题**(错答复演示、答对进阶,全过 praise 结课;保留直接答题 + 返回地图出口);Choice/ListenChoice 删 requireVisitAll/visited 听齐提示;所有题(选一选/听一听/短教/连连看)选项**恒 4**(engine `optionCountFor` 定 4、teach-questions 干扰 3);题卡左上角题型徽章(`TypeBadge` 选一选/听一听/连连看);ListenChoice 标题行恢复喇叭图标(Volume2)点重听(替虚线圆重听区);删孤立 `demo-blocks.*`(演示产物已随 demo/tap 退役)。已并入 main(commit e04260e),随 0.2.0 发 — [quiz-interaction spec](superpowers/specs/2026-09-06-quiz-interaction-redesign-design.md) / [foundation spec](superpowers/specs/2026-09-05-foundation-learning-design.md)
- [ ] `P0` `[feature]` 远程 D1 迁移 — 0.2.0 发布前按序 apply,preview → 生产;本地已 `npm run db:local`。⚠ **2026-09-16 更正(原写「apply `0002_fun.sql`」已过期)**:main 上的迁移是 `0001`–`0007`(截至 2026-09-16 更正时;个数不写死,用 `git ls-tree --name-only main migrations/ | grep '\.sql$'` 自查 —— 本分支另含 `0008_pinyin_progress`,合并后该数字即变) —— `0002_fun`(settings 新列)/ `0003_basics` / `0004_chinese_domain` / `0005_qianzigu_chapter` / `0006_sentence_step` / `0007_bonus_granted`;其中 **0004–0007 属 0.3.0 工作但代码已在 main** → **照当前 main 发布 = 0.2.0 会一次带上 0.3.0 的全部表**。**发布前必须裁定**(二选一):①接受并同步修正「0.3.0 未发」口径;②先把 0004–0007 相关代码从 0.2.0 发布范围摘出。不定 = 远程库与版本口径对不上
- [x] `P0` `[feature]` 发音首响优化(预热·防掐头·静默暖机)— 0.2.0 人工验收:发音有延时、偶发前半段无声。speech 服务:创建即预取 voice 表 + `voiceschanged` 刷新缓存(空轮询不覆盖好缓存);语音未就绪时最新一条朗读入队、就绪补播(不静默丢);空闲不 cancel 立即播、仅引擎忙才 cancel 且隔 ~30ms 再播(防 Chrome 吞句头);无匹配 voice 且引擎有 voices 时降级默认音;**不做引擎暖机**(取舍 2026-09-09:曾播 volume=0 真音节想无声唤醒懒 TTS,Firefox/Chrome 不遵守 volume=0 → 会话首次交互真实响 `'a'`(进千字谷地图 bug),已整体删除;voice 预取/入队/防掐头保留)。测试 speech 4→9、`npm test` 264 绿 — 并入 main,随 0.2.0 发
- [x] `P0` `[feature]` 声调锚统一 qi 四调 + 删轻声 — 拼音短教声调单元锚改同一音节 qi:ton1 七 `qī` `7️⃣` / ton2 旗 `qí` `🚩` / ton3 企 `qǐ` `🐧` / ton4 气 `qì` `🎈`;轻声 ton0 整体退役(本无出题、decompose 只顺带产键记过)→ `PINYIN_TONES` 4 项、轻声音节不再产声调单元键,帽子/月亮等 ~21 轻声音不再为幽灵单元强制补教 — 已并入 main(commit 008bfa8),随 0.2.0 发

- [x] `P0` `[feature]` 特效零文本化(撒花 / 连击 / 成就 / 幸运奖励)+ 地图徽章栏 — 2026-09-23 产品口径「撒花、连击等特效,不能有中文,小孩子根本看不懂的,需要重新设计」。**立项时诊断(2026-09-23;下列**诊断结论**是本支实施前的实况;块内凡**现在时态的具体状态描述** —— 例如「今天那页只有退出/重置」「地图今天不读 settings」这两处括注 —— **同样一律是实施前的实况**,以本行末「实施后实况」为准(例外:行内「**进 0.2.0**」那句不属此列 —— 它**今天仍为真**);块内的**裁定本身** / 不在射程 / 与想法池的关系**今天仍成立**(裁定是决定,不是状态))**:孩子面中文只有两处 —— `AchievementPopup`(「解锁成就」+ 成就名 + 说明 +「+50 星尘」)与 `LuckyBonus`(「好运来了!」+「灵灵在草丛里找到了一颗隐藏星尘!」);另有三条:**① `LuckyBonus.tsx:20` 的「灵灵」是活的假话** —— 苏灵灵已随「拼音积木取代全游戏」删除,全仓唯一残留 `lingling` 是 `speech.ts:9` **登记保留、零生产调用**的声纹,该句今天**无指称对象**;② `celebrate` 四档**只有 `achievement` 真接线**(`step` / `word` / `combo10` 是**声明了没人调**的死档);③ **连击在孩子面零反馈** —— `combo` 只静默进 `starDust`。**裁定(2026-09-23 产品已选)**:两个弹层去字 → emoji + 撒花 + 撒花规模;**数字保留**(「+50」留,只删「星尘」二字);连击加**关内圆点**;成就名/说明**搬到家长面板**(今天那页只有退出/重置)—— 文字不删只挪到成人面;**地图新增徽章栏**(数据新接 `settings.earnedAchievements`,地图今天不读 settings)。**不在射程**:家长面板 / 登录门 / 错误 toast 的中文(给大人用的)。与想法池「关卡内星级即时反馈」行**相邻但不合并** —— 本行**不含**逐关 1–3 星的出口(徽章栏是成就栏,不是星级栏)。**进 0.2.0**:0.2.0 未发、未 push,分开发 = 明知假话先上线一次。**实施后实况**:两个弹层已零文本(孩子面中文仅剩答案行的同音汉字,教学内容);`LuckyBonus` 那句「灵灵」已删;`celebrate` 四档全部接线(`step` 更名 `combo5`);连击有关内圆点;地图徽章栏已接 `settings.earnedAchievements`;成就名/说明的出口在家长面板。— 已实施(plan `docs/superpowers/plans/2026-09-23-effects-textless.md`),随 0.2.0 发

### feature 轨 — 目标 `0.3.0`(未发;新能力,兼容 → minor)

> ❌ **整轨失效(2026-09-21)**:被上行「拼音积木取代全游戏」整体取代。本轨全部条目的处置对象 —— 千字谷章节与故事线、词课三技能、句型步、答题卡壳与糖果材质、故事屋世界观 —— **随词课与千字谷一起删除**,不再执行。
> 其中两条的通用教训**并入新行验收**:①「台词从待教的词倒推」(来自「ch1 故事打磨」)②「Tailwind 扫描器陷阱:材质类必须走 `index.css` 普通类,否则 test/lint 全绿而只有 build 暴露」(来自「糖果材质 + 爽感层」)。
> 旧行**就地留痕不删**(防反复),其失效标记下不再追加维护。
> 📌 2026-09-22 台账清理:故事台账 `docs/design/`(故事圣经 / game-direction / game-assets / game-visual)与 `docs/superpowers/plans/` 的旧计划已随「拼音积木取代全游戏」T14a **整目录删除**;本文件其它行里引用它们的链接/路径仅作历史留痕,已失效。

> ⚠ **2026-09-16 轨内账目**:本轨 9 条**三种状态混用**(「开发完成待发」/「spec 已出待评审」/「未立项」)。按 PLAN 规则「排进本轮开工 → 升当前迭代」,**无 spec 者不占本轮** —— 现存 1 条违规(见「句型步题型改造」行,已就地标注处置待裁)。另:**本轨实际被 `0.2.0` 阻塞**(迁移账见 `0.2.0` 轨「远程 D1 迁移」行)。— ⚠ **2026-09-22 复核**:本轨现有 **10 行**(上句的「9 条」已过期),且整轨已被上行新行取代(见轨首横幅);本条账目仅作历史留痕。

- [ ] `P0` `[feature]` 汉语领域并轨 S1 — 启用模型 3 技能开关 → 2 领域开关(汉语=拼音+汉字 捆绑 / 英语),内部 `SkillKey`/进度三键/步序/出题/结算不变;面板与冷启动按域;迁移 `0004_chinese_domain.sql` 加 `enable_chinese`(回填任一侧旧汉语技能开即域开)。随 0.2.0 发布后下一条 feature 轨发 — [spec](superpowers/specs/2026-09-08-chinese-domain-merge-design.md) / plan `2026-09-08-chinese-domain-merge.md`(已删)
- [ ] `P0` `[feature]` 千字谷镇重设(全拟人角色 + 轻线悬念,ch1 内容重写)— 全游戏收敛为汉语世界;班底 苏灵灵🦊 / 徐万年🦥 / 皮小闹🦝 / 吴铭🦇(绰号谐音「无名」,小名小明,真名常默=末章一剧情词);**无通用群众位** —— 每个出声角色都有姓名与一页卡,后续新角色按 spec §4.6 规程逐个立项;结构保留(幕数经下行 ch1 重构扩为 23)、引擎/进度零动、零位图(emoji);`SpeechRole` 终态 5 值(旧天体角色 / 黑影反派 / 群众位一律退役;语音域红线,已随 spec 报批);旧 ch1 进度作废。**文档已同步**:`game-story-bible.md` / `game-direction.md` / `game-assets.md` / `game-visual.md` 已按落地重写(含 4 张角色一页卡),`docs/design/` 命名残留复核 0 命中(PLAN `[x]` 历史条目保留原样)— ⚠ **2026-09-22 更正:该断言今天与事实相反** —— 这四份与整个 `docs/design/` 已随「拼音积木取代全游戏」T14a **整目录删除**(那句话只在 2026-09-10 当时成立,今天这四份文档**不存在**,本行**不再有文档锚点**);发布前闸门 = 浏览器走查 23 幕 — [design](superpowers/specs/2026-09-10-qianzigu-town-redesign-design.md) — ⚠ **部分失效(2026-09-16)**:结构(23 幕)/ 引擎 / 目录名保留,**角色集与镇设定被「故事屋重设」取代**(下行)。**勾选状态说明**:本行留 `[ ]` 是因「浏览器 23 幕走查」闸门未跑(无 headless 工具),**非代码未完成** —— 与下行 `[x]` 的「千字谷 ch1 纵切片」不矛盾,两者是同一批已落地代码的「代码完成」与「验收未跑」两面
- [ ] `P0` `[feature]` 千字谷 ch1 重构(高潮 + 笑点 + 句型步切片 + 干扰项场景化)— 2026-09-11 产品口径「重构第一章,要有高潮和笑点」:①**章章有高潮有笑点** —— 消费想法池「章节叙事闸门」行,本章交付 = 章节契约补「高潮位 / 笑点位」两栏 + 剧情重写(现 ch1 笑点已在「推门拉反 / 皮小闹撞人」,高潮偏弱需重排);②**句型步先行切片** —— ch1 五词各配**多句、由易到难**,「声 → 形 → 句」三步连排(全量 100 词留想法池 P1);③选择题干扰项取**本章场景词 / 已学复习词**(替同 category 随机词,`question-engine` 接收场景词池入参)。⚠ 撞数据模型(word progress `sentence` 键 + 迁移)/ 幕数契约(23 幕、断点重算)/ 出题引擎 —— **须先过 game-pipeline 阶段 0 重锚 + spec,勿直接开工**。— 开发完成待发 — [design](superpowers/specs/2026-09-11-qianzigu-ch1-rebuild-design.md) — ⚠ **部分失效(2026-09-16)**:②**句型步切片** / ③**干扰项取本章场景词** **已落地 main 且保留**(取证:`qianzigu/ch1.ts` 23 幕含 `sentence` 层、`question-engine/engine.ts:47` `QuestionContext.sceneWordIds`、`qianzigu/ChapterRunnerView.tsx:221` 已传 `chapter.wordIds`);①**剧情重写(高潮位/笑点位/ch1 台词)被「故事屋重设」取代**。⛔ **同改 `src/features/qianzigu/ch1.ts` —— 禁与「故事屋重设」并行**
- [ ] `P1` `[feature]` 花仙国世界观重设(外层世界 + 魔法=咒语 + 角色语境)— 2026-09-11 产品口径「重新设计故事圣经」:新增外层国度**花仙国**与浮空花形巨城**花仙城**(熊猫贵族居所,掌政治/经济/军事/粮食),千字谷降为**城下平民小镇**,字母林保留英语域;魔法 = **咒语即词**,完整沿用「叫不出名字就使不上劲」+ 双层恢复(声音层拼音 → 形状层汉字);主题命题换为「名字叫得响,再平凡的人也有了力量」(旧命题降为情感目标);调性 = **温暖成长童话**,阶级只作底色,**贵族无台词不出场** —— `SpeechRole` 五值不动,**不触语音域红线**;4 张角色一页卡只改「身份」语境,人设/外貌锚冻结。**零代码红线**:引擎/进度/判分/语音/词库零动;ch1 三处锚点台词**须并入上行 ch1 重构批次执行,禁并行改同一文件**;下游 `game-story-bible` / `game-direction` / `game-assets` / `game-visual` 四档同步。**欠账**:吴铭「伤口/弧光终点」仍待全篇框架定。— 阶段 0 已重锚,spec 已出待评审,实现押后 — [design](superpowers/specs/2026-09-11-huaxian-kingdom-redesign-design.md) — ❌ **已失效(2026-09-16)**:被「故事屋重设」取代,实现未开始,直接作废
- [x] `P0` `[feature]` 千字谷 ch1 纵切片(已落地,发轨待 0.3.0)— 重构现游戏为千字谷:双世界壳(千字谷·章节地图 / 字母林保留零改动);ch1《太阳的求救》可玩纵切片(词 1/101/102/103/3,story 补词 升起/亮/早上好;内容已由 2026-09-10 镇重设整体替换,见上行),P3 引擎跑 dialogue/task(听音·辨形两层恢复,minCorrect 2)/社交(两段式先听后选)/BOSS/结局/结算,恢复写词进度 + 复用星尘结算(幂等),断点续玩落迁移 0005。5-plan 经 SDD 全绿(324 tests,0 Critical);架构红线(useService 单点/feature 不互引/worker 行级)全守。**发布前闸门**:spec §7 浏览器人工走查(灰白→彩色 / 全程角色语音 / 刷新续玩 / BOSS 失败保留 / 社交听选)——无 headless 工具未自动。绘画/部件拼装子项未含(见想法池)。已解禁边界见「坚决不做」。— [spec](superpowers/specs/2026-09-08-qianzigu-ch1-design.md) / plans `2026-09-08-qianzigu-ch1-*`(5 份:vocab / speech-role / ch1-engine / ch1-persistence / ch1-ui,已删)

- [ ] `P0` `[feature]` 题面舞台化(答题卡壳 → 游戏演出壳)— 2026-09-15 产品口径「题目的 UI 需要重新设计,要求更像是游戏,而不是选择题」:三个 signifier 同改 —— 题干居中大标题 → **气泡说话框**、2×2 等距网格卡 → **错落词石**、提交钮「确定」→「**就它了!**」;另去外层白卡(试卷纸感放大器)+ 补**色彩冗余**(对错加 ✓/✗ 角标 —— 现 `Choice` 的 reveal/wrong 是纯色表达,一并修缺口)。**皮肤层**:交互仍「点选 + 确认」,不引拖拽;**引擎 / 进度 / 判分 / 语音 / `Question` 数据形状零动**,题型组件契约不变 → 视觉统一发生在 `shared/ui/quiz/` 内部。范围 = 词课 / 千字谷跑章 / 短教 / 冷启动 **4 消费点全改**;素材零新增(emoji + 现有 token);减动效靠既有 `MotionConfig reducedMotion="user"` 自动继承。⚠ 措辞变更破 **9 个测试文件**的「确定」断言;短教须兑现本文件上行「教学期视觉壳与真答题区分」承诺(改**虚线边框 + 卷轴头**双通道,不靠颜色)。— 开发完成,待 0.3.0 发 — [design](superpowers/specs/2026-09-15-quiz-stage-reshell-design.md)
- [ ] `P0` `[feature]` 句型步题型改造(4 选 1 长句 → 词组句)— 2026-09-15 产品口径「题目要和故事完美融合;不要选句子的选择题,太长了,不适合小朋友」:句型层 `makeSentenceQuestions` 现出「哪句话说对了?」+ 4 个整句选项(档 3 最长 14 汉字 ×4 句),对儿童过重。改 Duolingo 式**词组句** —— 已选区 + 词块池(词块复用 `shared/ui/quiz/stone.ts` 三态),点块入槽、**严格判唯一正确序列**(词块 id 区分,重复块不互相覆盖)。**范围只到句型层**:声(拼音)/ 形(汉字)两层维持选一选 / 连连看 —— `layerToSkill` 下声/形考读音与字形,**一个词本身拼不出句子,词组句不成立**。数据:每档**手写** `tiles`(有序 = 答案)+ `extra`(干扰块,不引分词库,守「依赖精简」)。契约:`SentenceItem` 换形状 + `QuestionKind` 新增 `'build'` + `BuildQuestion` + 新 `shared/ui/quiz/BuildSentence.tsx`(+测试);`TypeBadge.QuizKind` 是**第二个重复定义的 union** 须同步;`WordLesson.tsx` `renderQuestion` switch **无 default、无 never 断言 → 加守卫**,否则新 kind 静默返回 `undefined`(现行地雷)。⚠ 撞 `Question` 契约(`shared/services/`,消费方 question-engine / lesson / qianzigu / foundation)→ **须先出 spec,勿直接开工**;`SentenceSet`(`vocabulary.ts`)与 `SentenceTextSet`(`question-engine.ts`)是**结构重复定义**,一并收口。数据面 15 档全重写 + `sentences.test.ts` 六条守卫全绑旧「1 正 3 错句」形状须重写;新守卫 ① `tiles` 拼接 === `correct` ② `extra` 与 `tiles` 零重复(否则严格判分歧义)③ 词块数硬线 ≤5。估算 M(1~2d)→ 独立 minor。— ⚠ **状态违规(2026-09-16 标)**:本行原写「spec 未出,入池立项」却占 `0.3.0` 轨位,与 PLAN 规则「排进本轮开工 → 升当前迭代」冲突(无 spec = 未立项 = 不占本轮)。**处置待裁**:补 spec 升轨转正,或回想法池。**保留 P0 与原地不动** —— 移轨会改优先级语义(产品决策,非整理可决)。**依赖**:与下行「千字谷 ch1 重构」`ch1.ts` 重叠(5 个 `t*-sentence` 的 `intro`),而该行①已被「故事屋重设」取代 → **本行落地前须先确认新 ch1 的 5 词与句型数据是否重写**
- [ ] `P0` `[feature]` 千字谷 ch1 故事打磨(弧光重摆 · 高潮铺垫 · 打动人的地方)— 2026-09-15 产品口径三轮:「这个故事存在问题」「没有什么亮点,太普通了」「缺少能打动人的地方」。**诊断(取证见 `ch1.ts` / bible §3.1)**:①圣经给徐万年的 11 栏**只用 2 栏**(声纹 / 忘事)——身份「镇上修东西的人」、want「把手上没修完的东西修完」、「谎言「东西还在,只是我没找到」、伤口「年轻时全镇人的名字都背得出」**全空**;而弧光终点「接受被帮,说出第一句『我忘了』」在 `ch1.ts:36`(`open` 第 9 行)**已经达成**,后 20 幕零变化 → 他不是角色,是「忘事」机制的载体;②高潮位 `boss.win`「还有你——苏灵灵!」**无铺垫** —— 全章他对苏灵灵互动一直正常,从未演过「他眼里没有别人」;③苏灵灵**零阻力**,need「不帮忙也值得被记住」一次未碰;④**logic bug**:两人同住千字谷镇,`t1-sound` 却问「你家在哪儿呀」,而 `t2-sound` 她自己说「到啦!这就是你家!」—— 两幕自相矛盾(症状:台词从待教的词倒推,人物在服务词表)。**处置**:**弧光三态重摆** —— `open` 嘴硬(说出圣经那句没用过的谎言「我没事,东西还在,只是没找到」)→ `t1-sound` **真慌一次**(「我好像,不知道往哪走了」,「不知道路」≠「我忘了」,不吃掉后续台阶)→ `t3` 第一次说出「忘」字 → `t5` 灯亮说出「我忘了」;**删 `ch1.ts:36`**;**今天的事** = 他要修好那盏台灯、天黑前点亮,`t5` 从「天黑了」变「**这一天成了**」;**高潮铺垫** = `open` 他叫不出苏灵灵的名字,`boss.win` 回收;**苏灵灵第一次摩擦** = 被他挡回,`t3` 才被接受;**她的伤口不露**,只做「全章唯一注意到徐爷爷不对劲的人」(留弹药给后续章);**social 换事件**(撞人 → 皮小闹「帮忙」帮出祸,笑点机制由「夸张」改**「落差」**,自动落回白名单);**`mood` 3 → ~10 处**;**`ending`/`settle` `day` → `night`**(`stage-meta.ts:30` 一行,修 t5 唯一真夜戏被结局抹平 + 3 个夜断点的天空矛盾);**`BreakScene` 文案去时间语义**(`scene-ui.tsx:30`「天黑了」与「天黑前修好」期限直接冲突;单 feature,红线外);**`boss.lose` 2 → 3 行**收束;**伏笔触达**登记为有意设计(钩子 = 通关奖励)写进 bible §6。**红线守**:引擎 / 进度 / 判分 / 语音 / `Question` 形状**零动**,`SpeechRole` 五值不动,**幕数 23 不动**。文档层须**先行**同步(bible §1/§3.1/§4/§5/§6 + ch1 重构 spec §6.1/§6.2/§6.3 + `game-direction.md`)。⚠ 与上行「千字谷 ch1 重构」(开发完成待发)同改 `ch1.ts`,**禁并行**;与「句型步题型改造」交叠面 = 5 个 `t*-sentence` 的 `intro` 一行 → **先落题型改造,本行随后一起把 5 句 intro 改 diegetic**,反序返工;本轮改动若先于 0.3.0 发布落地,则上行「千字谷镇重设」的**浏览器 23 幕走查闸门须重跑**。估算 M(1~2d)→ 独立 minor。— spec 未出,2026-09-15 入池立项 — ❌ **已失效(2026-09-16)**:arc 诊断有价值但**处置对象(徐万年/苏灵灵)整体退役**,本行不再执行;唯一保留物 = 「台词从待教的词倒推」这条通用教训,并入「故事屋重设」验收复核
- [ ] `P0` `[feature]` 题面糖果材质 + 爽感层(第二刀:解决「太丑」,承「题面舞台化」)— 2026-09-15 产品口径「做题的界面还是太丑了,能不能弄一个好看炫酷的 UI/UX?」:经浏览器并排 mockup 六轮定案 **C 糖果立体 · C3 满配** —— 词石改**糖果厚块**(实心底边 = 厚度);新增 `shared/ui/quiz/Stone.tsx`(词石唯一渲染点,消掉 Choice/MatchGame 既有重复)/ `SparkBurst.tsx`(答对迸星)/ `ProgressCrystals.tsx`(水晶进度条,替 3 处重复圆点) + `features/lesson/LessonAmbience.tsx`(景深);**emoji 柔光晕托**修「彩色位图不吃 `color`」的系统性撞色(🌙 压橙底 / 🍏 压绿底 / 🍎 压红底 —— 上一轮只解了「底 vs 舞台」,没解「emoji vs 底」);**连连看 = 炼金 + 爆点**(配对 → 右拼音熔进左词石成金星石两行、右位留虚线凹槽;瞬间对撞 + 冲击波白环 + 「啪!」)。**材质走 `index.css` 普通类**(`var(--color-*)` + `color-mix`,禁裸 hex;仅金色三主题恒定)—— 规避上一轮 Tailwind 扫描器陷阱(抽 helper → 产物规则归零、test/lint 全绿、只有 build 能暴露),且暗色模式由 token 自动接管。**锁死**:交互仍「点选 → 就它了!」,**不引拖拽**;**引擎 / 进度 / 判分 / 语音 / `Question` 形状零动**,题型组件只增可选 `quiet`(听一听不迸星,由 `ListenChoice` 内部消化 → 4 消费点零改)。**砍全屏震屏** —— 「震屏须有显式开关」是无障碍硬要求,要加得动 settings 模型 + 迁移,不值。**夜色题区不跟舞台换皮**(发光浮岛),承上一轮「对比度与舞台解耦」。**金色文字用实测 token 禁 `opacity`**(`#4a2e00` @72% 压金底实测仅 4.01:1)。数据改动仅 1 处:`WordLesson` 加必传 `dust: number` + `LessonEntry` 传值。分档表见 spec §6(景深只有词课 —— 短教/冷启动是全屏浮层,无天空)。— 已实施,待 0.3.0 发 — [design](superpowers/specs/2026-09-15-quiz-candy-juice-design.md)
- [ ] `P0` `[feature]` 故事屋世界观重设(《爱丽丝梦游仙境》为第一本 · 儿童绘本形式)— 2026-09-16 产品口径「重新从零开始设计故事的世界观,不要先有的任何设定,要求改成儿童故事绘本的故事形式,以爱丽丝梦游仙境为故事内容,不要自己想故事了,没有一个可以打动我的」:**三轮原创故事(《做蛋糕》/ 凳子呼应 / 风车)全部作废,本设计不再产出原创情节**。①**框架** = 魔法语言岛上的「**故事屋**」,一面书架,每本书 = 一个世界 = 一章;「每章独立、章间无联系」由**书架本身**承担(不再靠旁白圆场);第一本 =《爱丽丝梦游仙境》。②**玩家模型与旁白位零改** —— 绘本的天然结构是「有人念给你听」:旁白📖 = **念故事的人**(现职责一字不动),玩家 = **听故事的人**(现 canon「无名搭档/无背景/无立绘/无台词/角色对『你』说话」逐条命中)。③**引擎从 Carroll 原生主题长出来**:原著通篇「你是谁/这是什么」(第五章毛毛虫那问)→ 规则①重述为「书里的东西答不出名字就不认得你」,规则②三层恢复**零动**,旧规则③(字即物之名)**退役**。④**ch1 = 原著第一章**(追白兔 → 掉进兔子洞 → 金钥匙/小门/变小/变大),因果链**全部出自原著、零编造**;5 词候选 门(7)/钥匙(14)/桌子(90)/蛋糕(24)/+1(**待定**:钟 9 怀表 或 杯子 15,Carroll 原文是「怀表/小瓶子」,词库只有「钟/杯子」—— **须 spec 阶段定,不得留给实现**)。⑤**角色集替换** —— 苏灵灵🦊/徐万年🦥/皮小闹🦝/吴铭🦇 **全部退役**(**吴铭轻线悬念作废**,`settle` 钩子删),ch1 新班底 **4 值**:爱丽丝🎀/白兔先生🐇/柴郡猫😼/旁白📖;帽匠/红心王后/毛毛虫等**按章增量**,直接复用 bible §3.4 现成规程逐个立项(**不重写规程**);本章**不设反派**。⚠ **触 `SpeechRole` 语音域红线(5 值 → 4 值),本行即报批件**;旧角色字面量**曾散落在已删的多个 feature 里**(`SpeechRole` 那批角色值),须全量清零 —— ⚠ **2026-09-22 复核**:随本轮整批删除,现今只剩 `speech` 相关的几个文件(`features/speech/speech.ts` 与 `speech.test.ts`、`shared/services/speech.ts` 与 `speech.contract.test.ts`),且是**登记保留**的 `speakRole` 面(见 [dev-reference](dev-reference.md)「发音与关卡数据约定」),不是待清零项。**条数不写死**(今天真、明天未必 —— 它与代码同步变),当场跑 `git grep -lE "lingling|xuwannian|pixiaonao|changmo|narrator" HEAD -- src worker` 取数(去掉 `HEAD` 即查工作树)。⑥**零动**:引擎/进度/判分/语音实现/`Question` 形状/词库 100 词/**23 幕结构契约**/每章 5 词×3 层。⑦旧 ch1 进度作废(比照镇重设 D11,不写迁移)。⑧**视觉形态改「舞台 → 纸页」**(旁白朗读体为主、翻页感、绘本句式韵律)—— 属 `game-visual-design` 域,`Scene`/`ChapterLine`/`StageMeta` 数据形状不动。**两条风险已报备**:①**素材成本为本次最贵项** —— 绘本形式 = 每屏一张插画、一章 23 屏、20 章 460 屏,现全 emoji 零位图 → 分期(一期 emoji + 纸页版式,二期插画走 `stage-visuals.ts` 现成接缝);②**惊艳度**来自 Carroll 原生**身高失控喜剧**,**不追求**「意料之外×情理之中」翻面 —— 产品已知悉接受,后续若要求翻面须回头改 spec §5.1,**禁中途塞戏**。**⚠ emoji 撞库项 —— 2026-09-16 自我更正:由「高危未裁定门」降级为「低危 · 登记即可」**:白兔先生🐇 撞词库 43 兔子🐰、柴郡猫😼 撞词库 41 猫🐱,码点不同故按字面不违规。**原判据「干扰项全库 shuffle」经复核不成立** —— 「题干干扰项场景化」**已落地**(`question-engine/engine.ts:47` `QuestionContext.sceneWordIds` + `engine.ts:59-67` 池序 场景词→复习词→同类→跨类;`qianzigu/ChapterRunnerView.tsx:221` 已传 `chapter.wordIds`)→ **ch1 干扰项恒取本章 5 词,兔子🐰/猫🐱 进不了 ch1 题面,「同一屏同时看到二者」不成立**。**残余风险 = 跨屏**:孩子在词课学过 兔子🐰,后在故事里见 白兔先生🐇,可能分不清是否同物 → 处置:**登记为「有意设计」并落 `game-assets.md`**(靠名字与声纹区分);视觉侧试看若仍判混淆,再启用备选(白兔→⌚ 怀表 / 柴郡猫→💫)。**取代 / 部分取代**:取代本轨「花仙国世界观重设」行、本轨「千字谷 ch1 故事打磨」行;**部分取代本轨「千字谷 ch1 重构」行**(②句型步切片 / ③干扰项场景化**已落地 main 保留**,①剧情重写被本行取代)、**部分取代**「千字谷镇重设」行(结构 / 引擎 / 目录名保留,角色与镇设定作废)。⛔ **串行红线(接盘自已失效的『千字谷 ch1 故事打磨』行)**:本行与「千字谷 ch1 重构」**同改 `src/features/qianzigu/ch1.ts`,禁并行** —— 本行整体重写该文件,须待上行落地后**单开 topic 分支 + worktree** 执行。**下游四档同步**:`game-story-bible.md`(§0/§1/§2/§3/§4/§6 重锚,旧条目标「已失效」**不删**)/ `game-direction.md` / `game-assets.md` / `game-visual.md`;台词最后写、只进 `ch1.ts` 并过 `ch1-script.test.ts`。**评审方 ≠ 创作方**,台账入库 `docs/design/`(禁 `docs/.tmp/`)。— spec 已出待评审 — [design](superpowers/specs/2026-09-16-storybook-house-alice-design.md)

### hotfix 轨 — 目标 `0.1.1`(基于 `v0.1.0` 已发 tag)

- (空 — `0.2.0` 发布前只修 `v0.1.0` 上线 bug → `0.1.1`;`0.2.0` 发布后 base 前移,下一 hotfix 为 `0.2.1`。浏览器验收 / 生产冒烟发现 bug 即在此加行 `[hotfix]`,独立快速发) — ⚠ **2026-09-16 账目提示**:`0.2.0` 轨的「远程 D1 迁移」未发,`0.3.0` 轨已堆 9 条(多条标「开发完成待发」)→ **`0.2.0` 是当前唯一瓶颈,其后全部堵住**;本轨 `0.1.1` 目标因此长期挂空。**发布节奏须先解 `0.2.0`(含其迁移账)**(2026-09-22 复核:该轨已整轨作废、行数亦非 9 —— 本条仅作历史留痕)

## 想法池

> 一行一需求,`P1` = 确认要做待排期,`P2` = 想法观察不承诺。行格式:`- [ ] P<n> 标题 — <一句上下文>`。重复即弃旧。
>
> ⚠ **2026-09-22 前提失效(拼音积木取代全游戏)**:本池相当一部分行以**已删对象**为前提 —— 词课 / 千字谷 / 字母林 / 基础引导 / 出题引擎 / 100 词词库 / `shared/ui/quiz/`。行内引用的源文件路径(`words.ts` / `sentences.ts` / `chapter-progress.ts` / `stage-visuals.ts` / `shared/ui/quiz/*` / `src/features/qianzigu/ch1.ts` …)与代码符号(`ROLE_META` / `STAGE_BODIES` / `restoreOrder` / `sceneWordIds` / `bonusGranted` / `fullComplete` / `distractorsFor` …)**今天都不存在**,`docs/design/` 四档文档也已整目录删除。**这些想法是留是退属产品裁定**,本轮只留痕不代裁;逐行的失效锚点清单见 `.superpowers/sdd/2026-09-21-pinyin-blocks-full-game/task-14a-r1-report.md`。

- [ ] `P1` 绘画题型 — 一词一画:拍照 → 缩略 → 亮度抠图 → 自动裁剪 → 确认;`progress.drawing_*` 列后续迁移加入(一期视觉来源 = `word.emoji`,`getVisual(word)` 已留接缝)。spec §2.2 有述
- [ ] `P1` 填空题型 — fillBlank:汉字补缺字 / 英语补字母(引擎候选题型 §7.2 已预列,编码未做)
- [ ] `P1` 复习挑战 / 错词重练排程 — 每 5 新词、弱技能优先出题
- [ ] `P1` 离线可用 — IndexedDB + Service Worker + syncQueue + 字段级合并
- [ ] `P1` settings 中途开启模块的语义缺口 — 家长学习时关技能、之后重开 → 已学词重达「全完成」再触发 +20、目标词回跳;以 per-word 永久 `bonus_granted` 列 + ever-enabled 解锁修复。spec §13 有述
- [ ] `P1` 词库 emoji / 分类语义人工校对 — shape 组含书 / 门 / 礼物等非形状物、nature 组含蜜蜂 / 蝴蝶等动物(源自 v1.1 数据瑕疵),需真人图文重整。— ⚠ **2026-09-16 隐含依赖(与「故事屋重设」)**:新 ch1 的 5 词中 **门(7) 正在 shape 组**,而本条要修的恰是 shape 组的语义错配 → **本条一旦落地把「门」移出 shape 组,新 ch1 的干扰项池会跟着变**(池序首段 = 同 category,`engine.ts:66`)。两者**须同批确认,勿一前一后各自改**
- [ ] `P1` 汉语句型步全量(每词多句 · 由易到难 · 拼装)— 原则「不止认字,还要帮孩子说话」:汉语域每词加第 3 步「声 → 形 → 句」,题型 = 句子模板挖空 + 4 选项点选成句(复用 `question-engine` 与可注入 `rng`);**不出声、不判音准** —— 守住「坚决不做:语音识别」红线。**每词多句、难度阶梯**(句长 / 结构由易到难),100 词各若干句**手写**(词库无词类,无法自动生成),句子存 `src/features/vocabulary/sentences.ts`(`wordId → sentence[]`,不塞 `words.ts`);选项干扰优先取**本章场景词 / 已学复习词**(见下行)。word progress 加 `sentence` 键 + 迁移;幕数变化、断点重算;**称号阈值重算 = 全量前置**(全量后每词含句步,整词 `+20` 与称号所需星尘口径须重算)。⚠ **须与 0.3.0「汉语领域并轨 S1」的 `0004_chinese_domain.sql` 合并设计**(句型归汉语域内固定步,不新增第 3 开关),勿两条迁移并行。不做整句乱序拼装(数据 ×3,留二期)。**ch1 五词先行切片已升 0.3.0 feature 轨**(见上行),本行留**全量 100 词**。工作量 L → 强制拆 spec。2026-09-11 入池,同日修订为多句阶梯,未立项
- [x] `P1` 题干干扰项场景化(替同 category 随机词)— 现状 `question-engine.ts` `distractorsFor` =「同 `category` 优先,不足跨类补齐」+ 全库 shuffle,ch1 场景下「房子」的干扰项可能抽到「鲸鱼 / 火箭 / 蛋糕」等与当下剧情毫无关系的词,孩子只能靠猜不靠懂。改:干扰项优先取**本章场景相关词**(同章已恢复 / 将学的 `wordIds`)+ **已学复习词**,场景池不足才回落到同 `category`。落点 = `question-engine` 接收「场景词池」纯数据入参(可注入 `rng` 不变),全章共用;词库学习岛主线亦受益。⚠ 与「汉语句型步」同批设计(句型 4 选项同用此策略)。2026-09-11 入池,未立项。— ✅ **已落地(2026-09-16 核实,本行关闭)**:`question-engine/engine.ts:47` 已有 `QuestionContext.sceneWordIds`,`engine.ts:59-67` 池序 = 场景词 → 已学复习词 → 同 category → 跨类补齐(各段独立 shuffle、按序拼接取前 `count`,`seen` 去重);消费点 `qianzigu/ChapterRunnerView.tsx:221` 已传 `chapter.wordIds`;测试 `engine.test.ts:264-300`。⚠ **账目冲突留痕**:`0.3.0` 轨「千字谷 ch1 重构」行同时记本项为「开发完成待发」= **同一功能两笔记账**,以本行为准关闭
- [ ] `P1` 章节叙事闸门(章章有高潮 + 章章有笑点)— 原则「每章要有跌宕起伏的高潮」「最好有一点孩子都能明白的笑点」:章节契约新增两栏必填 —— **高潮位**(哪一幕 / 情绪峰值 / 场面不可逆改变)与 **笑点位**(哪一幕 / 机制 = 误会·重复·落差)。**判据**:笑点禁谐音梗与双关(孩子词汇量接不住);填不出任一栏 = 不交稿。落点:`game-story-bible.md` §4 章节契约 + `game-story-design` 技能质检闸门 + 章节 spec 必填同两栏。ch1 追溯登记:高潮 = `t1-shape` 第一个小高潮 + `boss` win;笑点 = 徐万年推门拉反 + 皮小闹冒失撞人(现散在 `game-direction.md`,收进契约)。2026-09-11 入池,未立项。— ⚠ **追溯登记部分失效(2026-09-16)**:末句「ch1 追溯登记」所指角色(徐万年 / 皮小闹)与场景(`t1-shape` / 推门拉反 / 撞人)**已被「故事屋重设」整体作废**;落点 `game-story-bible.md` §4 将随重锚改写。**机制部分仍然有效且已生效**(章节契约新增「高潮位 / 笑点位」两栏 + 判据;故事屋 ch1 已按此填两栏)—— 填新 ch1 追溯时**重写本段,勿引旧值**
- [ ] `P1` 笑点机制白名单裁定与扩容 — 2026-09-11 技能评审(`game-story-design` §十二 笑点配方)发现上行判据有三笔账:①**ch1 自身破格** —— `2026-09-11-qianzigu-ch1-rebuild-design.md` §6.3 把笑点 3「皮小闹撞人」登记为「**夸张**」,不在上行白名单 {误会·重复·落差} 内;②**上行 ch1 追溯登记与 spec 不一致** —— 上行记 2 处(推门拉反 / 皮小闹撞人),spec §6.3 记 3 处(+ `open` 徐万年卡壳);③**白名单撑不住连载** —— 仅 3 个机制,而 ch1 已吃掉「重复」(卡壳)与「落差」(拉反),ch2 只剩「误会」;全篇 20 章「章章有笑点」将在第三、四章必然重放。处置二选一:**扩白名单**至技能 §十二 机制库(落差 / 重复变奏 / 误会 / 错位 / 夸张 / callback / 角色驱动,仍禁谐音与双关)或**保留窄白名单 + 逐章轮换登记**。**阻塞**:ch1 重构 spec 定稿(笑点位冻结)与 ch2 立项均依赖本裁定。落点:`game-story-bible.md` §4 章节契约 + ch1 spec §6.3 + 本文件上行判据。2026-09-11 入池,未立项。**2026-09-15 部分解**:账 ① 将由 0.3.0 轨「千字谷 ch1 故事打磨」清除 —— 该批把 social 事件整体换掉(撞人 → 皮小闹「帮忙」帮出祸),笑点机制由「夸张」改**「落差」**,自动落回现白名单;账 ②(登记处数不一致)随之重写。**账 ③(白名单撑不住 20 章连载)仍待裁定**,ch2 立项前必须还。— ⚠ **2026-09-16 更新**:账 ① 的原定解法(靠「千字谷 ch1 故事打磨」把「撞人」换成「帮出祸」)**随该行失效而作废**;但**答案不变** —— 「故事屋重设」的 ch1 笑点位已全部落在白名单内(重复:白兔先生「来不及了」/ 落差:身高失控 / 误会:柴郡猫答非所问),账 ① 自动清零,账 ② 随之重写。⛔ **账 ③ 不但仍待裁定,对「故事屋」反而更紧** —— 每章一本新书,笑点池**不随世界观重置**,20 章连载的机制枯竭问题更早到来,**仍须在 ch2 立项前还**
- [ ] `P2` mergeProgress 死导出 — ❌ **2026-09-22 作废**:原条目设想的对象 `src/game/progress.ts`(连整个 `src/game/`)已随「拼音积木取代全游戏」整批删除 → 设想没有对象了,原来那条链接也已指向不存在的文件。留痕不删
- [ ] `P1` 发音引擎替换为 kokoro 本地 TTS(替浏览器 SpeechSynthesis)— 目的:跨设备一致 zh/en 真声 + 书中多角色独立嗓音(现仅 rate/pitch 硬调;⚠ **2026-09-16 前提变更**:原写「千字谷**五**角色」,随「故事屋重设」`SpeechRole` 由 5 值 → **4 值**,`SPEECH_ROLE_VOICE` 须同步);接缝单点 = `src/features/speech` 内 `SpeechService` 换实现,`AudioService`(音效)不动。未定:推理跑哪(端侧 WASM kokoro-js / 出包预生成音频资产 / 本机 sidecar——生产为 Cloudflare 托管 → sidecar 基本不可行);词库+ch1 台词文本固定宜缓存/预生成,但 ch1 台词仍打磨中 → 预生成时机未到。2026-09-09 入池,未立项
- [ ] `P1` 千字谷 场景两态升级 — 待 ch 数据带 scene-elements(村庄/月亮/天空元素)后按 spec §3.3 全场景灰度→点亮 + ending 白闪,并接线 `celebrate`(现死)/消费 `restoreOrder`;当前 = 顶部 SkyStrip 逐词点亮(纵切片最小实现);★ 灰白→彩色点亮核心子集已随「跑章漫画舞台化」P1 行(下)推进,逐元素精确编排等仍留本行后续 — [spec](superpowers/specs/2026-09-09-qianzigu-manga-stage-design.md)
- [ ] `P1` 千字谷 绘画/部件拼装子项 — 拍照→亮度抠图→贴图进场景(P1 绘画题型同源)+ 汉字部件拖拽拼装;ch1 竖切按 spec §3.0 跳过,接缝已标 R4a 留缝
- [ ] `P1` 千字谷 跑章漫画舞台化 — 千字谷跑章视觉重构:全窗即舞台 + 同框多人(数据驱动 `cast`)+ 角色旁动态气泡对话 + 氛围灰白→彩色点亮;引擎/进度/语音零动,`stage-visuals.ts` 留位图接缝;本轮部分消费「场景两态升级」行(见其注)— [spec](superpowers/specs/2026-09-09-qianzigu-manga-stage-design.md)
- [ ] `P2` 千字谷 漫画视觉语言增强 — (跑章舞台化后的后续子集)借鉴漫画风格设计蓝图:对白气泡**情绪造型**+按文本自适应(呐喊锯齿/思考云泡/字号)、**拟声词艺术字**即时弹出(BOOM!/咚咚,重击/答对爆点)、**镜头语言**(motion 特写/推拉/抖振做情绪与词点亮强调)、角色**站位朝向阵营感/三分法**编排;待 Plan2/3(stage-scenes + ch1 数据)合入后评估 —— React 2D 落地 = CSS + motion,DialoguePresenter/StageFrame 上扩展,引擎零动;台词泡/同框/氛围点亮已有(勿重建)。来源:漫画风格游戏设计蓝图会话贴入 2026-09-09
- [ ] `P2` 千字谷 花仙城天幕元素 — 花仙城作**天幕远景剪影**加入 `STAGE_BODIES`(+ index.css token,改映射表,组件零改);归属「花仙国世界观重设」的视觉落点,前置低但**须立项再动**(勿跳过变更闸门)。同时待裁决:苏灵灵「服装/道具」栏(学徒气质?),须视觉侧定稿后回写 bible 卡,禁由 AI 跑图反向当设计。2026-09-11 入池,未立项
- [ ] `P1` 千字谷 新章存档校验硬编码 ch1 词表 — `chapter-progress.ts` `load()` 恢复校验写死 `CHAPTER_1.wordIds`(源码注释已标此坑),ch2+ 落地后**新章存档会被静默作废**(玩家看到的 = 进度凭空没了)。修 = 按 `remote.chapterId` 解析对应章节词表,**ch2 前置项**;与「ch1 重构」同行评审(幕数 / 断点变动会连同重算校验)。2026-09-11 入池,未立项
- [ ] `P2` 千字谷 章节 done 标记 / 结局回放 — settle 现即 `chapter.clear` 清行重玩,完成度由 per-word 承载;「已通关重看结局」需 done 列
- [x] `P2` 千字谷 双语 wordBonus 口径 — 默认 enableEnglish 下 `fullComplete` 含英语域,ch1 只写汉语域 → 整词 +20 恒不发(故事词 101-103 字母林永不涉足);「千字谷仅汉语域」的 +20 判定待产品对齐(full-review N1)。**2026-09-11 随 ch1 重构批次裁定**:口径已定 — ch1 侧 trigger = `chapterWordDone`(拼音 + 汉字 + 句满),**不吃 `settings`**,英语域不参与本域整词完成 → 双语下 +20 正常发;跨路径只发一次由**持久化支付位** `WordProgress.bonusGranted`(迁移 `0007_bonus_granted.sql`)保证,不从完成谓词反推(任一派生判据都会双发或吞发)。详见 ch1 重构 spec §8。原「全量句步后称号阈值重算」遗留归「汉语句型步全量」行
- [ ] `P2` 千字谷 UX 打磨 — neutral 社交选项误播 wrong 音(N2)/ BOSS 题形恒 choice(N3,题序词池×[pinyin,hanzi] 轮转)/ 地图「继续」标签(有断点仍显「开始」)/ 重登落点定夺(回上次世界 vs 每次回世界壳,现 lastMapRef 保留)
- [ ] `P2` 千字谷 世界侧代价(灰度)— 每未恢复/答错一词让山谷灰度加深一档,替代「玩家惩罚」(儿童向);纯世界侧张力,【未定】需 scene `stage` 新字段 + 引擎读取 → 撞「引擎零动」红线,**须先立项再动**。来源:故事评审 2026-09-10(草稿 `docs/.tmp/story-review.md`,本地不入库)
- [ ] `P2` 千字谷 社交选项独立演出 — `social-pixiaonao` 的 bad/neutral 现仅回 `loop` 重弹(有意设计 = 教育性重试);候选升级 = 各给一次性独特演出后仍引导回正轨(不阻塞、保持单解可通);触及社交场景数据与 UI 取舍逻辑,【未定】须先立项。来源:故事评审 2026-09-10(草稿 `docs/.tmp/story-review.md`,本地不入库)。— ⚠ **2026-09-16 前提变更**:本条写死的 scene id `social-pixiaonao`(取证 `qianzigu/ch1.ts:123`)**随「故事屋重设」改变**(该位换柴郡猫);**机制不变**(坏 / 中性分支现仅回 `loop` 重弹 = 有意设计,教育性重试),落点 id 须重填
- [ ] `P2` 千字谷 角色立绘位图化 — 现全角色 = `ROLE_META` 单 emoji;素材 prompt 已**设计前置定稿**(`docs/design/game-assets.md`:4 张角色立绘 prompt(角色集 5 值,旁白不产素材)+ 布景元素 + 氛围 5 档 + 情绪符号 + 三无质检);回填须动 `ROLE_META`/CastFigure 渲染层(spec §10 位图升级),撞「语音零动」边缘,**须先立项再动**。来源:角色重设 2026-09-10。— ⚠ **2026-09-16 前提作废**:本条引的「4 张角色立绘 prompt(角色集 5 值)已**设计前置定稿**」对应**旧角色集**(苏灵灵 / 徐万年 / 皮小闹 / 吴铭),随「故事屋重设」整体作废 → `game-assets.md` 须按新 4 值(**爱丽丝🎀 / 白兔先生🐇 / 柴郡猫😼 / 旁白不产素材**)**重出 prompt**;**出 prompt 前本条不动**
- [ ] `P2` 图卡按钮缺 accessible name — 连连看右列图卡的图 `aria-hidden`、而引擎对右列选项的 `text` 恒为空串 ⇒ 读屏听到的是无名按钮;基线 `41ea069` 即存在(非「题面糖果材质」计划引入),该计划的无障碍闸门只覆盖布局/对比/动效、未覆盖控件可访问名。修法二选一:UI 侧给卡片补 `aria-label`(不动引擎),或引擎为图选项补可读文字(⚠ `question-engine` 的 `Question` 数据形状属零动红线,须先立项)。2026-09-16 入池,未立项,未确认要做
- [ ] `P2` 千字谷 演出账补齐 — `mood` 现 **3 处**(social 惊吓 / social 收尾 happy / boss 胜利 happy,原「全章 0 处」随 ch1 换写作废);余下待办 = 节奏实测回填(现仅 2s/5s 起步值,未测);分镜/交互现状与待办清单见 `docs/design/game-direction.md` §4。来源:角色重设 2026-09-10。— ⚠ **2026-09-16 数字待重算**:本条「`mood` 现 **3 处**」及其指涉(social 惊吓 / social 收尾 happy / boss 胜利 happy)属**旧 ch1**,随「故事屋重设」重写后须**重新取证回填**;`game-direction.md` §4 同批重写
- [ ] `P2` 关卡内星级即时反馈(连击 / 星级揭晓)— spec `2026-09-21-pinyin-blocks-full-game-design.md` §7.4 原本要求「拼齐 → 星级揭晓 → 「继续」按钮」,本版未实现(拼齐后自动推进、末关自动回地图;星只在地图格子上有点/灭两态,看不出 1–3 星);产品决定,未立项
- [ ] `P2` 幸运奖励无自己的撒花档 — `CelebrateLevel` 四档(`combo5` / `word` / `combo10` / `achievement`)**没有幸运档**:`LuckyBonus` 只收 `{amount,onDone}`(无 celebrate 钩子,`App.tsx` 也没传),而 `LevelEntry` 的守卫(`result.achievements.length === 0 && result.luckyReward <= 0`)在幸运接手时把 `word` 让掉 → **弹了幸运的那一关撒花一记都没有,比普通通关还安静**。spec `2026-09-23-effects-textless-design.md` §3.4 触发点表本就**只列三档**、无幸运档,故这是**缺口**、不是回归。两种收法待定:①给幸运加一档(`CelebrateLevel` + `CONFIGS` + `LuckyBonus` 加 celebrate 钩子);②改守卫条件、让幸运不吞 `word` 档。**须先立项再动**(动手即改孩子可见行为)
- [ ] `P1` 走查默认「走查前账号是全新的」这句话没写下来 — **根因**:整份 `docs/walkthrough.md`(尤其 §B)默认走查者手上是**全新账号**,但**这句话从未写在文件里**;而 `.claude/skills/release/SKILL.md` 的闸门①流水线(步骤 2–5)里**没有任何清库 / 复位步骤**,preview 又是 `wrangler.toml` 里那个**具名常驻 D1**(`[env.preview]` 的 `jazz-life-tracker-preview`,独立 `database_id`)。**受影响的行**(现象逐条核过):`W-C1` / `W-C8` 期望写死「只有第 1 单元可点」「第 2 单元的 🔒 消失」这类**全新态**事实,已通过关的账号上不成立;`W-M1` 前提写死「只有 u1 可点」,而按走查 §D 第 3 条的顺序(发布前 = §B 核心回归,合并前 = §B + §C 全量)`W-C8` 一走完 u2 就解锁 ⇒ 到该行前提已假、操作做不了,**且行内没有「未验」出口**;`W-L2` 操作要求进到有双音节词的关卡(`src/features/pinyin-blocks/levels.ts` 的**最后一个**单元,u7)⇒ 空库上走不到,**同样没有出口**;`W-P3`(重置)前提「先确保已有点亮的星星和星尘」冷启动不满足(代价小,玩一关即可)。**两个候选方向**:①走查侧 —— 给这几行补前提(「账号须是全新的,旧进度先走重置」)或补「做不到 ⇒ 记「未验」」出口;②流水线侧 —— 在 `release` skill 里补一步「走闸门①前先重置 preview 进度」,把「全新库」变成**有据的环境事实**。**为什么不在本支修**:这几行**都是分支前既有**的,本支只新增了 `W-C9` / `W-V5`(两者在空库上都安全)⇒ 按「本支引入的就地修、分支前既有的只登记」这条分界线,属**登记不修**;真要动会同时触及走查多行 + 发布 skill,**须先立项**。来源:Task 8 修复轮 R3 的定向复审扫出(2026-09-23),未立项

> **已废弃(2026-09-10)**:「五世界」方向被 D1「全游戏收敛为汉语世界」取代,详见
> [千字谷镇重设 spec](superpowers/specs/2026-09-10-qianzigu-town-redesign-design.md)。原节内容见 git 历史。

### 独立想法(原五世界节内,与方向无关)

- [ ] `P2` 英语 5 层教学结构(音素→拼读→高频词→句型→对话)— 现英语=逐词单词,改分层路径影响出题引擎与词库
- [ ] `P2` 词库分级 + 每词多维度字段(核心100全量/扩展/补充;hanziDetail radical·family、phonics、drawSteps、melody 等)— words.ts 模型扩展 + 大规模数据生产
- [ ] `P2` 屏幕时间守卫 — 家长可配单次/每日时长,到点结算并鼓励休息;需 settings 新列 + 迁移 + 运行计时与打断 UI
- [ ] `P2` 分角色分技能学习报告 — ⚠️ 与坚决不做「家长看板」冲突;需先解禁该条

## 坚决不做

> 非目标,留痕防反复。设计已划界,避免待办蔓延。原 `CLAUDE.md`「真不做」+ `README`「明确不做」并集。

- 语音识别
- 汉字书写笔顺(**边界 2026-09-08**:不做笔画顺序书写教学;汉字**部件拖拽拼装**放行,千字谷 ch1 用)
- 绘画作品检测(**边界 2026-09-08**:不做绘画内容的语义识别/理解;拍照→亮度抠图→贴图放行,千字谷 ch1 用)
- 多孩子档案
- 家长看板
- 商店 / 徽章 / 宠物 / 每日挑战
- PWA
- 多人在线 / 社交
- 用户 ID 展示
